# static/

이 폴더의 파일은 빌드 시 사이트 루트로 그대로 복사된다.
`static/resume.pdf` → `https<도메인>/resume.pdf`

## 주의

저장소가 public이므로 여기 두는 파일은 전부 공개된다.
검색엔진에도 노출되고, 커밋한 뒤에는 파일을 지워도 git 히스토리에 남는다.
이력서를 올릴 때는 전화번호·주소·생년월일 같은 항목을 빼고 올릴 것.
